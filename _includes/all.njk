<html>

<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
<link rel="shortcut icon" type=image/png href="http://rsna19.aiai.bio/static/favicon.png">
<title>{{ title or pkg.name + ' — ' + pkg.description }}</title>
<style>
body {
  margin: 0;
  padding: 0;
  min-height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;

  font-family: system-ui, sans-serif;
}

body > * {
  max-width: 40em;
}

footer {
  margin: 3em 0 0;
  border-top: 1px solid #ccc;
}

p.tiny {
      font-size: 0.5em;
}

a:link {
      color: #888;
      text-decoration: none;
}
a:hover {
      text-decoration: underline;
}

</style>
</head>

<body>
  <main>
  <h1>{{ title or pkg.name }}</h1>
  <h2>{{ pkg.description }}</h2>
  {{ content | safe }}

  <ul>
  {%- for url in collections.urls -%}
    <br><a href="{{ url.data.url }}" target="_blank">{{ url.fileSlug }}</a>
      <p class="tiny">&nbsp; {{ url.data.url  }}</p>
  {% endfor %}
  </ul>

  <footer>
    <p></p>
  </main>
</body>

</html>
